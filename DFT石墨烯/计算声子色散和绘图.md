
# 生成 3×3×1 的超胞（石墨烯仅在xy平面，z方向不扩）
phonopy -d --dim="3 3 1" -c POSCAR-unit


phonopy --fc vasprun.xml               # 生成 force_constants

phonopy -c POSCAR-unit band.conf -p         # 生成 band.yaml

phonopy-bandplot --gnuplot band.yaml > band.dat  # 生成 dat 数据
phonopy-bandplot -o band.pdf band.yaml           # 生成 pdf 图片
